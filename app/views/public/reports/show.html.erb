<div class="row content-body">
  <%= render "layouts/sidebar" %>
  <div class="col-md-9 col-lg-8">
    <div class="text-center mt-5">
      <h5>Thank you for caring for the community!</h5>
      <p>よりよいコミュニティ作りに貢献いただきありがとうございます。</p>
      <small>
        Your report has been submitted on <%= @report.created_at.in_time_zone(current_user.time_zone).strftime('%m/%d/%Y %H:%M') %></br>
        あなたの通報は既に受理されています。
      </small>
    </div>
    <div class="text-center mt-5">
      <%= link_to "Home", home_path, class: "btn btn-sm btn-outline-dark" %>
    </div>
  </div>
  <%= render "public/searches/search" %>
</div>
